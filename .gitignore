# Rust
/target
**/*.rs.bk
Cargo.lock.bak

# sqlx offline cache (committed except for the per-developer scratch)
.sqlx-scratch/

# Local env / secrets
.env
.env.local
.env.*.local

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/

# Build artifacts
*.tar.gz
*.zip
dist/
target/distrib/

# ---------------------------------------------------------------------------
# Planning / process artifacts (specs/, .sdd/, discovery/, CLAUDE.md) are
# TRACKED as of 2026-07-23 — the owner reversed the earlier local-only policy.
# Stale editor backups of those docs stay out.
# ---------------------------------------------------------------------------
*.backup

# Claude Code project config is tracked (owner policy 2026-07-23), but
# per-user local settings and the harness-managed live git worktrees are not.
.claude/**/*.local.json
.claude/worktrees/
.midnight-expert/

# Brainstorming visual-companion scratch (mockups; not part of the project)
.superpowers/

# Node dependencies (regenerable; never commit)
node_modules/
