# Phase 11 — US10: Ledger reconcile & settle — retro

**Goal**: daily three-source comparison, loud drift alarms, watermark-idempotent batched burn.
**Independent Test (owner-gated)**: seeded ledger vs pre-prod vault: clean → equality report + burn; injected discrepancy → alarm, no burn; interruption → no double-burn.
**⚠️ GATE (T172)**: design the orchestrator-only BURN CIRCUIT via mnm/MNE (record addendum in research.md) BEFORE implementing; `/midnight-verify:verify` before merge.

## Constitution discipline (NON-NEGOTIABLE)
- **Burn circuit = compact-core:compact-dev + mnm/MNE + midnight-verify, NEVER memory.** The orchestrator-only AUTH is the hard design question — Compact has no `msg.sender`; do NOT use the `ownPublicKey()`-for-authorization anti-pattern (compact-security). Use a VERIFIED auth pattern (a signature / hash-derived authority / contract maintenance authority — research it). Real burn tx is OWNER-GATED (needs the deployed vault + orchestrator key on the local devnet); the simulator suite is the deterministic deliverable.
- **D55/D56:** the reconcile job burns vault NYXT matching consumed credit since the last watermark, EXACTLY ONCE per watermark (idempotency mirrors the deposit D45 / deploy tx_ref pattern). Daily schedule, cadence a config tunable (D56).
- **SC-039 zero-user-path:** the reconcile job MUST NOT be reachable from the user request path (background job only) — a static dependency audit.

## Task ledger status
- [x] T171 retro (this file)
- [x] T172 ⚠️ GATE — burn circuit DONE + `/midnight-verify:verify` **Confirmed by execution** (compact-dev agent). `export circuit burn(amount: Uint<64>, watermark: Bytes<32>): []` in `packages/nyxt-vault/`. **Orchestrator-only auth = witness-secret authority-commitment (the zkloan pattern), NOT `ownPublicKey()`:** `witness orchestratorSecret()` (32-byte private secret) → constructor pins `sealed orchestratorAuthority = persistentHash(domain-sep, secret)` → `burn` re-derives + asserts equality inside the ZK proof (only the secret-holder passes). **Burn primitive = `sendUnshielded` to all-zero UserAddress** (no `burnUnshieldedToken` in stdlib — verified), no auto-receive, vault net balance −amount; over-burn guarded by `unshieldedBalanceGte`. **Watermark idempotency = `burnedWatermarks: Set<Bytes<32>>`** (member→insert) — on-chain leg 3 of the SC-037 exactly-once, aligns with the reconcile `BurnExecutor` contract. Check+full-ZK compile exit 0; simulator 38/38 (16 deposit unchanged + 22 burn). Design addendum recorded in research.md (T172). Real burn E2E owner-gated.
- [x] T174 failing→green tests: watermark idempotency / zero double-burn under crash-replay (SC-037), drift-injection alarms no auto-correct (SC-038), EC-48/49 — `apps/server/tests/reconcile/reconcile.test.ts` (14)
- [x] T175 reconcile job (`ledger/reconcile.ts`: three-source compare on finalized watermark, two drift signals loud-alarmed + never auto-corrected, watermark-idempotent batched burn D55, persisted reports FR-069, EC-48 skip+reschedule+alert-after-N, EC-49 burn-fail watermark-unmoved, EC-50 finalized-watermark) + `ledger/reconcile-scheduler.ts` (daily D56 cadence primitive, 6 tests) + `PgReconcileStore`/`InMemoryReconcileStore`/`pgLedgerTotals` + migration 0004 (drift/burn_amount → numeric(40,0), the P8 money-width carry) + index.ts wiring (owner-gated seams, mirrors deploy-wallet)
- [x] T177 SC-039 zero-user-path static audit — `apps/server/tests/reconcile/no-user-path.test.ts` (4): reconcile imported ONLY by index.ts + scheduler; off the ledger barrel; no route/handler/coordinator/router/events/app import
- Server: 460 passed / 52 skipped (was 450→460, +24 reconcile: 14 core + 6 scheduler + 4 audit; +5 pg-gated). eslint/prettier/tsc clean.

## Build notes (reconcile core — built on main thread after 2 background-agent API failures)
- Two consecutive `devs:typescript-dev` dispatches died mid-research (API "connection closed" + 600s watchdog stall) writing NOTHING; built reconcile.ts directly to avoid bouncing off the flaky background path. Burn circuit stayed delegated (constitution-I).
- **Exactly-once (SC-037) = belt-and-suspenders** mirroring deposit D45: (1) `getRun(watermark)` short-circuit, (2) `reconcile_runs.watermark` UNIQUE 23505→already-done, (3) the on-chain burn circuit's watermark idempotency (BurnExecutor contract: a re-submitted watermark RESOLVES with the original txRef, never double-burns — covers the crash-between-burn-and-record window).
- **Burned high-water = the settlements figure of the most-recent `outcome='reconciled'` run.** A drift/error run does NOT advance it (EC-49: burn-fail → error row → next run retries the full delta; drift → next clean run burns the drift-period consumption too).
- **Two drift signals:** credits-vs-chain (`onchainDepositTotal − ledgerCredits`; chain may LEAD credits within `lagTolerance` for deposit-observation lag, but the ledger out-crediting the chain is ALWAYS drift = tampering) + vault-vs-expected (`vaultBalance === onchainDepositTotal − totalBurned`, strict). Either off → drift alarm, no burn, `outcome='drift'`.
- **Scheduler NOT auto-skip-looping noisily:** started at boot but its owner-gated `watermarkSource` rejects each tick → logs one `tick-error` per cadence (honest "armed but gated"); the job's on-chain seams are never even reached until the chain adapter lands.

## Owner-gated (build-around, flagged) — server side
- The finalized-watermark source, `onchainDepositTotal`, `vaultBalance`, `executeBurn` seams are rejecting owner-gated stubs in index.ts (need local devnet + deployed vault + orchestrator burn key + real indexer). `pgLedgerTotals` (Source 1) is real.
- No vault-address config field added THIS pass (no deployed vault this run; the on-chain seams that would consume it are owner-gated stubs). Left as a documented gap for when the bootstrap deploy lands.

## Owner-gated / flagged (build around)
- T172 burn auth design + real burn tx — owner-gated (research via mnm; real burn needs the deployed vault + orchestrator key on the local devnet).
- Real three-source reconcile (indexer + vault balance) — owner-gated; build against mockable seams.

## Decisions / spec (from extraction — quote-accurate)
- **FR-066** reconcile = daily scheduled job (config cadence), idempotent, NEVER in a user-facing path (D13/D56).
- **FR-067** compare THREE sources — (1) append-only Postgres ledger credits/settlements, (2) finalized on-chain deposit log (same indexer feed that credits deposits = Σ vault `deposits` map), (3) vault NYXT balance; drift alarms LOUDLY + MUST NOT auto-correct.
- **FR-068** batched burn = consumed credit since last watermark, EXACTLY ONCE per watermark, orchestrator-only auth on the burn circuit (D55).
- **FR-069** every run → persisted queryable report (inputs, totals, actions, outcome) = the `reconcile_runs` row.
- **SC-037** zero double-burn across interruption/replay (watermark advances exactly once per burned batch). **SC-038** injected drift detected+alarmed 100%. **SC-039** zero user-path (static dep audit — no user endpoint invokes reconcile code).
- **EC-48** indexer unavailable → skip + reschedule + alert after N consecutive skips (config), never a partial comparison. **EC-49** burn tx fails → report failure, retry next run, watermark UNMOVED (no double-burn). **EC-50** settlement lag → compare on a FINALIZED watermark, never wall-clock now. **EC-51** drift alarm during sessions → sessions unaffected (Postgres truth), operator-facing.
- **Seam state:** `reconcile_runs` table exists (watermark UNIQUE = idempotency key); NyxtVault `deposit`/mint exist (burn ADDED, no existing auth → orchestrator-only is greenfield T172); `foldBalance`/`getEntries` per-account (need vault-global aggregate); `observeFinalized` = exactly-once reference (write-first-then-flip); `reconcileCadenceMs` config exists; NO vault-address config (gap — US10 adds); scheduler greenfield; indexer/vault-balance/deposit-total = greenfield owner-gated `()=>Promise<bigint>` seams (mirror deploy `BalanceQuery`).

## Review outcomes — dual code review (sonnet+opus) + compact security review + fixes
**Burn circuit security review (compact-core:security-reviewer): NO Critical/High — auth pattern SOUND.** Confirmed the witness-secret gate is a real ZK binding (not the `!= const` no-op), domain-sep adequate, `sealed` prevents rotation, secret confidentiality preserved, blast radius = NYXT only (never tNIGHT). Fixes applied: **M1** — witness now rejects an all-zero/default secret (an unsalted public commitment made a default-secret deploy a universal burn bypass); **L1/L3** doc notes (burn = vault-balance reduction not total supply; redeploy-on-compromise runbook). L2 (bind `kernel.self()`) DEFERRED — the reviewer's own follow-up flagged a construction-vs-burn `kernel.self()` mismatch risk; neutralised operationally by a distinct per-deploy CSPRNG secret (M1 guidance). The compact-dev fix agent hit the **weekly usage limit** mid-investigation, so M1 (plain-TS witness) + the doc notes were applied on the main thread + re-verified (compact:check + 39 sim tests green).

**Server reconcile — both reviewers converged on ONE money-critical hole (C1/EC-49):** an ambiguous or concurrent burn retried under a DIFFERENT watermark defeats the on-chain `burnedWatermarks` dedup → silent double-burn (undetectable, absorbed into totalBurned). Owner-gated executor means latent, not live. **Fix = canonical per-period watermark contract + ambiguous-burn OPS GATE:** a burn-failed OR post-burn-record-failed writes an `error` row (carrying the attempted amount) and every subsequent run BLOCKS (loud `burn-unresolved` alarm) until ops resolves it — NEVER auto-retries under a fresh watermark (mirrors the deploy pipeline's ops-gated ambiguous outcome). Resolution (owner-gated ops): mark `reconciled` (landed → counts in totalBurned) or clear (didn't land → retry the full delta). Other fixes: **H1** bounded record-retry + `record-failed` result; **P (liveness)** scheduler first-tick from elapsed-since-last-run (scale-to-zero `min_machines_running=0` would otherwise reset the daily countdown every restart → job never fires); **M3** scheduler generation token (no double-arm on stop→start); **#3** pg-test `ledger_entries` poisoning (two bigint-safe halves summing past 2^63-1 instead of one un-narrowable row — was breaking the live down-migration test); **M2/O** BurnExecutor atomicity + watermark-encoding contracts documented; **L5** SC-039 audit now catches dynamic `import()`; **N** `lagTolerance` non-negative guard.

**Deferred/documented (Low, owner-gated or minor):** L6 persist-both-drift-signals (alarm carries full detail live); L7 cadence-lower-bound; L8 EC-48/blocked alarm re-fires per tick (ongoing visibility, acceptable); advisory-lock defense-in-depth (canonical watermark + belt-3 already money-safe).

**Commit `d5cd4ea`; PR #12 → main (open).** Server 469 / web 225 / nyxt-vault 39 / protocol 80 / scaffold 22. Lint/format/typecheck clean.

⚠️ **Note (weekly usage limit):** hit 2026-07-14 (resets Jul 17 11am Europe/Lisbon) — subagent dispatch is rate-limited; the C1/EC-49/M1 fixes + this review round were applied on the MAIN thread (no re-review subagent). A re-review of the fix diff is advisable when the limit resets before merge.
